import React from "react";
import styled from "styled-components";

function Image() {
  // prop destruction

  // lib hooks

  // state, ref, querystring hooks

  // form hooks

  // query hooks

  // calculated values

  // effects

  // handlers
  return <StyledImg></StyledImg>;
}

const StyledImg = styled.img``;

export { Image };
